@ECHO OFF
call npm install -g npm && call npm install -g node && call npm install -g nodemon && call npm install -g bcrypt && call npm install -g mongoose
@ECHO Update Complete! You may now use the Run.bat
PAUSE

